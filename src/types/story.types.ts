/**
 * Story types — Re-exported from shared.
 * @version 3.0.0
 */
export type { StoryType, Story, StoryAuthorInfo } from '../shared';

// Backward-compatible alias (old mobile code uses CarStory)
export type { Story as CarStory } from '../shared';
