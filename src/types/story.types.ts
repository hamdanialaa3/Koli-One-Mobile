/**
 * Story types — Re-exported from @koli-one/shared.
 * @version 3.0.0 — Unified with shared/
 */
export type { StoryType, Story, StoryAuthorInfo } from '@koli-one/shared';

// Backward-compatible alias (old mobile code uses CarStory)
export type { Story as CarStory } from '@koli-one/shared';
