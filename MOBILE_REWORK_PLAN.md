# Koli One Mobile — خطة التطوير الشاملة
**تاريخ التحليل**: 15 فبراير 2026  
**المحلل**: Claude Opus 4.6  
**الإطار**: Expo 54 + React Native 0.81.5 + Expo Router + Firebase + Algolia + Zustand

---

## الوضع الحالي — ملخص التدقيق

### ما يعمل بشكل جيد ✅
| الميزة | الحالة | التفاصيل |
|--------|--------|----------|
| البحث + الفلاتر | كامل | Algolia مع fallback لـ Firestore، فلاتر متقدمة |
| معالج البيع (7 خطوات) | كامل | BasicInfo → TechnicalSpecs → Equipment → Photos → PricingContact → AIDescription → Review |
| SmartSell (AI من الكاميرا) | كامل | كاميرا → تحليل AI (Cloud Function) → معاينة → تعبئة تلقائية للمعالج |
| الرسائل الفورية | كامل | Firebase RTDB في الوقت الحقيقي |
| تفاصيل السيارة | كامل | معرض صور، مواصفات، فحص VIN، تقدير السعر، سيارات مشابهة |
| الإشعارات | كامل | Push notifications مع deep linking |
| المصادقة | كامل | Email/Password + Google + مجهول |
| الملف الشخصي + لوحة التحكم | كامل | إحصائيات حقيقية من 7 مجموعات |
| المراجعات | كامل | نظام مراجعات مع خط إشراف |
| دليل التجار | كامل | بحث + فلترة من مجموعة dealerships |
| حاسبة التمويل | كامل | حسابات محلية (بدون خادم) |
| 17 قسم للصفحة الرئيسية | كامل | قابلة للتكوين من Firestore |
| TabBar مخصص | كامل | رسوم متحركة + BlurView + أيقونات متحركة + Badge رسائل |
| حالة المعالج المستمرة | كامل | Zustand + AsyncStorage |

### مشاكل حرجة 🔴
| المشكلة | الشدة | الموقع |
|---------|-------|--------|
| **AI Advisor مزيف** | 🔴 حرج | `app/ai/advisor.tsx` يستخدم `setTimeout` + `getAIResponse()` مشفر — لا يتصل بـ Cloud Functions |
| **مقارنة السيارات بيانات وهمية** | 🔴 حرج | `app/compare.tsx` يستخدم `SAMPLE_CARS` مشفر — لا يتصل بـ Firestore |
| **الخريطة بدون خريطة فعلية** | 🔴 حرج | `app/map-search.tsx` يعرض `MapPlaceholder` — `react-native-maps` غير مستخدم |
| **Analytics معطل (403)** | 🔴 حرج | Firebase Analytics يرجع 403 Forbidden — النطاق غير مضاف |
| **لا طريقة للتواصل مع البائع** | 🔴 حرج | لا WhatsApp ولا اتصال هاتفي ولا نموذج تواصل |
| **لا ضغط للصور** | 🟡 مهم | `useUploadQueue` يرفع بدون compression أو validation |
| **لا حفظ تلقائي للمسودات** | 🟡 مهم | المسودات تختفي عند إغلاق التطبيق |
| **لا مشاركة للإعلانات** | 🟡 مهم | لا يوجد زر مشاركة في أي شاشة |
| **Stories بيانات وهمية** | 🟡 مهم | `SAMPLE_STORIES` fallback عندما تكون Firestore فارغة |
| **console.error مخالف** | 🟡 مهم | في `WizardOrchestrator` — يخالف قاعدة no-console |
| **نظام رسائل مزدوج** | 🟡 مهم | `MessagingService` (RTDB) + `ConversationService` (Firestore) — تناقض محتمل |
| **flag الـ AI Advisor معطل** | 🟡 مهم | `AI_ADVISOR: false` لكن الشاشة قابلة للوصول |

---

## خطة التنفيذ — مرتبة حسب الأولوية

### Sprint 0: إصلاحات حرجة فورية (P0) — هذه الجلسة

#### 1. ربط AI Advisor بـ Cloud Functions
- **ملف**: `app/ai/advisor.tsx`
- **المشكلة**: `setTimeout` + `getAIResponse()` مشفر
- **الحل**: استبدال بـ `httpsCallable(functions, 'geminiChat')` الموجود في الخادم
- **التأثير**: تحويل من ميزة وهمية إلى ذكاء اصطناعي حقيقي

#### 2. ربط مقارنة السيارات ببيانات حقيقية  
- **ملف**: `app/compare.tsx`
- **المشكلة**: `SAMPLE_CARS` مشفر
- **الحل**: إضافة `CompareService` يجلب سيارات من `ListingService` + واجهة اختيار

#### 3. تفعيل الخريطة الفعلية
- **ملف**: `app/map-search.tsx`
- **المشكلة**: `MapPlaceholder` المكون
- **الحل**: `react-native-maps` موجود في `package.json` — تفعيل `MapView` الفعلي مع markers من البحث

#### 4. إصلاح Firebase Analytics
- **المشكلة**: 403 Forbidden
- **الحل**: التحقق من تهيئة `AnalyticsService` — تجاوز الخطأ في بيئة التطوير

#### 5. إضافة أزرار التواصل مع البائع
- **ملفات**: `app/car/[id].tsx`
- **الحل**: إضافة `ContactSellerBar` مع: اتصال هاتفي + WhatsApp + رسالة في التطبيق

#### 6. خط أنابيب ضغط الصور
- **ملف**: `src/hooks/useUploadQueue.ts`
- **الحل**: إضافة ضغط قبل الرفع — `expo-image-manipulator` أو `ImageManipulator`

### Sprint 1: ميزات مهمة (P1) — بعد P0

#### 7. خدمة حفظ المسودات التلقائي
- إنشاء `DraftService.ts` يحفظ كل تغيير في المعالج إلى Firestore

#### 8. مشاركة الإعلانات
- Deep links + native share via `expo-sharing`

#### 9. خدمة المفضلات المحسّنة
- `FavoritesService.ts` مع UI سلس + optimistic updates

#### 10. تفعيل AI Advisor في feature flags
- تغيير `AI_ADVISOR: false` → `true`

#### 11. إزالة البيانات الوهمية من Stories

#### 12. إصلاح مخالفات console

### Sprint 2: تحسينات (P2) — لاحقاً

#### 13. شاشة تعديل الملف الشخصي
#### 14. وضع التقاط السريع (Quick Capture)
#### 15. تحسينات الأداء وقياسات KPI

---

## القرارات المعمارية

### Offline Strategy: Online-First + Persistent Drafts
**القرار**: لا حاجة لـ Realm/SQLite كامل. AsyncStorage + Zustand persist كافي.
**المبرر**: 
- بلغاريا لديها تغطية إنترنت ممتازة (4G/5G)
- التعقيد المعماري لـ Offline-First sync engine لا يبرره حجم المستخدمين الحالي
- Zustand persist موجود ويعمل بالفعل في `useSellStore`

### AI Strategy: Proxy via Cloud Functions
**القرار**: كل AI يمر عبر Cloud Functions الموجودة (لا API مباشر من الهاتف)
**المبرر**:
- الأمان: مفاتيح API لا تخرج من الخادم
- Quota: التحكم المركزي
- النقاط: `geminiChat`, `analyzeCarImage`, `geminiPriceSuggestion`, `evaluateCar`

### State Management: Zustand (الحالي — ممتاز)
**لا تغيير مطلوب**. Zustand خفيف ومتوافق مع React 19.

---
